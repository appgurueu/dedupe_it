name: dedupe_it
description: A speedy duplicate finder

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hash: ^1.0.1
  flutter_spinkit: ^4.1.2
  file_chooser:
    git:
      url: git://github.com/google/flutter-desktop-embedding.git
      path: plugins/file_chooser
      ref: 66e4b058971cb6c92c653f0d89c6a5c4aadb6539

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true